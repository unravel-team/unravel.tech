# PostHog Analytics Configuration
# Get your API key and host from https://app.posthog.com/project/settings

# PostHog API Key (required)
# Format: phc_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
PUBLIC_POSTHOG_KEY=phc_YOUR_API_KEY_HERE

# PostHog Host (optional - defaults to https://app.posthog.com)
# Use this if you're self-hosting PostHog or using EU cloud
PUBLIC_POSTHOG_HOST=https://app.posthog.com

# Feature Flags
# Enable/disable services pages and navigation (set to 'false' to hide from users and crawlers)
ENABLE_SERVICES=true

# Cloudflare Pages Deployment Configuration
# Get these values from your Cloudflare dashboard
# These are used by the root Makefile for deployment

# Cloudflare Pages project name
CLOUDFLARE_PROJECT_NAME=unravel-tech

# Cloudflare account ID (optional, only needed for cache purging)
# Find it in Cloudflare Dashboard > Account > Account ID
# CLOUDFLARE_ACCOUNT_ID=your-account-id

# Cloudflare API token (optional, only needed for cache purging)
# Create it in Cloudflare Dashboard > My Profile > API Tokens
# CLOUDFLARE_API_TOKEN=your-api-token

# Development Notes:
# - Prefix environment variables with PUBLIC_ to make them available in client-side code
# - Never commit your actual .env file with real API keys to version control
# - PostHog will work in development mode but logs to console
# - Cloudflare variables are used by the Makefile in the root directory for deployment
# - You can also set Cloudflare variables as system environment variables or pass them to make
