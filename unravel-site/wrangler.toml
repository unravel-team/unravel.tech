name = "unravel-tech"
compatibility_date = "2024-01-01"

# Pages build configuration
pages_build_output_dir = "dist"

# The build command should use npm run build, not astro build directly
# This ensures the astro CLI is properly resolved from node_modules/.bin/
[build]
command = "npm run build"

# Additional configuration for Cloudflare Pages
[env.production]
# Production environment variables can be set here or in Cloudflare Dashboard

[env.preview]
# Preview/staging environment variables
