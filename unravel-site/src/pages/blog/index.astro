---
import BaseLayout from '../../layouts/BaseLayout.astro';

const allPosts = await Astro.glob('./*.md');
const sortedPosts = allPosts.sort((a, b) => 
  new Date(b.frontmatter.pubDate).getTime() - new Date(a.frontmatter.pubDate).getTime()
);
---

<BaseLayout 
  title="Blog | AI Consulting Insights"
  description="Expert insights on artificial intelligence, machine learning, and enterprise AI implementation."
>
  <section class="blog-list">
    <div class="container">
      <div class="posts-grid">
        {sortedPosts.map(post => {
          const formattedDate = new Intl.DateTimeFormat('en-US', {
            year: 'numeric',
            month: 'long',
            day: 'numeric'
          }).format(new Date(post.frontmatter.pubDate));
          
          return (
            <article class="card post-card">
              <div class="post-card-content">
                <div class="post-card-header">
                  <h2 class="post-card-title">
                    <a href={post.url}>{post.frontmatter.title}</a>
                  </h2>
                  <p class="post-card-meta">
                    <span class="post-author">{post.frontmatter.author || 'AI Consulting Team'}</span>
                    <span class="post-date">{formattedDate}</span>
                  </p>
                </div>
                <p class="post-card-description">
                  {post.frontmatter.description}
                </p>
                {post.frontmatter.tags && post.frontmatter.tags.length > 0 && (
                  <div class="post-card-tags">
                    {post.frontmatter.tags.map(tag => (
                      <span class="tag">{tag}</span>
                    ))}
                  </div>
                )}
                <a href={post.url} class="read-more">
                  Read more &rarr;
                </a>
              </div>
            </article>
          );
        })}
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  /* Blog Hero */
  .blog-hero {
    background: var(--color-bg-gradient-vibrant);
    padding: var(--spacing-3xl) 0;
    text-align: center;
    position: relative;
  }

  .blog-hero::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: radial-gradient(at 50% 50%, rgba(14, 159, 188, 0.05) 0%, transparent 70%);
    pointer-events: none;
  }

  .blog-title {
    font-size: var(--text-4xl);
    margin-bottom: var(--spacing-md);
  }

  .blog-description {
    font-size: var(--text-base);
    color: var(--color-text-secondary);
    max-width: 600px;
    margin: 0 auto;
  }

  /* Blog List */
  .blog-list {
    padding: var(--spacing-3xl) 0;
    background: var(--color-bg-gradient-soft);
    min-height: 100vh;
    position: relative;
  }

  .blog-list::before {
    content: '';
    position: absolute;
    top: 50%;
    right: 0;
    width: 600px;
    height: 600px;
    background: radial-gradient(circle, rgba(108, 179, 63, 0.04) 0%, transparent 70%);
    pointer-events: none;
    transform: translateY(-50%);
  }

  .blog-list::after {
    content: '';
    position: absolute;
    bottom: 10%;
    left: 0;
    width: 500px;
    height: 500px;
    background: radial-gradient(circle, rgba(14, 159, 188, 0.04) 0%, transparent 70%);
    pointer-events: none;
  }

  .posts-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
    gap: var(--spacing-xl);
  }

  .post-card {
    display: flex;
    flex-direction: column;
    height: 100%;
    position: relative;
    z-index: 1;
  }

  .post-card-content {
    display: flex;
    flex-direction: column;
    height: 100%;
  }

  .post-card-header {
    margin-bottom: var(--spacing-lg);
  }

  .post-card-title {
    margin-bottom: var(--spacing-sm);
    font-size: var(--text-xl);
  }

  .post-card-title a {
    color: var(--color-text-primary);
    text-decoration: none;
    transition: color var(--transition-base);
  }

  .post-card-title a:hover {
    color: var(--color-primary-600);
  }

  .post-card-meta {
    display: flex;
    gap: var(--spacing-md);
    font-size: var(--text-xs);
    color: var(--color-text-tertiary);
    margin: 0;
  }
  
  .post-author {
    font-weight: 500;
  }
  
  .post-date {
    font-weight: 500;
  }

  .post-card-description {
    color: var(--color-text-secondary);
    margin-bottom: var(--spacing-lg);
    line-height: var(--leading-relaxed);
    flex-grow: 1;
  }

  .post-card-tags {
    display: flex;
    gap: var(--spacing-sm);
    flex-wrap: wrap;
    margin-bottom: var(--spacing-lg);
  }

  .tag {
    background: var(--color-primary-100);
    color: var(--color-primary-700);
    padding: var(--spacing-xs) var(--spacing-sm);
    border-radius: 12px;
    font-size: var(--text-xs);
    font-weight: 500;
    transition: all var(--transition-base);
  }
  
  .tag:first-child {
    background: var(--color-secondary-500);
    color: var(--color-neutral-000);
  }
  
  .tag:hover {
    transform: translateY(-1px);
    box-shadow: 0 2px 6px rgba(14, 159, 188, 0.2);
  }

  .read-more {
    color: var(--color-primary-700);
    font-weight: 600;
    transition: all var(--transition-base);
    align-self: flex-start;
  }

  .read-more:hover {
    color: var(--color-primary-600);
    text-decoration: underline;
  }

  /* Responsive */
  @media (max-width: 768px) {
    .blog-title {
      font-size: var(--text-3xl);
    }

    /* Hide decorative elements on mobile to prevent overflow */
    .blog-list::before,
    .blog-list::after {
      display: none;
    }

    .posts-grid {
      grid-template-columns: 1fr;
    }

    .post-card-meta {
      flex-direction: column;
      gap: var(--spacing-xs);
    }
  }
</style>
